import React from 'react'

// Should hold state for user's data
// Since dashboard will use all of user's active data, need api calls for active data (incomplete tasks, active habits, active routines, incomplete goals)

function AppMain() {
  return (
    <div>AppMain</div>
  )
}

export default AppMain